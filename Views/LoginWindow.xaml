<Window x:Class="SaveCodeClassfication.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Masin 세이브코드 분류 - 로그인" Height="520" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Background="#f5f5f5">
        <Border Background="White" Margin="20" CornerRadius="8" BorderBrush="#e0e0e0" BorderThickness="1">
            <StackPanel Margin="30" VerticalAlignment="Center">
                <!-- Header -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text="🔐" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Masin 세이브코드 분류" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333"/>
                </StackPanel>
                
                <TextBlock Text="로그인" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,25" Foreground="#2196F3"/>
                
                <!-- Login Form -->
                <StackPanel>
                    <TextBlock Text="아이디" FontWeight="Bold" Margin="0,0,0,5" Foreground="#555" FontSize="12"/>
                    <TextBox Name="TxtUserId" Height="38" Padding="12,8" FontSize="14" Margin="0,0,0,15" 
                             BorderBrush="#ddd" BorderThickness="1" Background="White" TextChanged="TxtUserId_TextChanged"/>
                    
                    <TextBlock Text="비밀번호" FontWeight="Bold" Margin="0,0,0,5" Foreground="#555" FontSize="12"/>
                    <PasswordBox Name="TxtPassword" Height="38" Padding="12,8" FontSize="14" Margin="0,0,0,20" 
                                BorderBrush="#ddd" BorderThickness="1" Background="White" PasswordChanged="TxtPassword_PasswordChanged"/>
                    
                    <Button Name="BtnLogin" Content="로그인" Height="42" FontSize="16" FontWeight="Bold"
                           Background="#2196F3" Foreground="White" BorderThickness="0" Margin="0,0,0,20"
                           Click="BtnLogin_Click" IsEnabled="False"/>
                    
                    <!-- 회원가입 링크 영역 -->
                    <Border Background="#f8f9fa" CornerRadius="6" Padding="15,12" Margin="0,0,0,25">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="계정이 없으신가요? " FontSize="14" Foreground="#666" VerticalAlignment="Center"/>
                            <Button Name="BtnRegister" Content="회원가입" FontSize="14" Foreground="#2196F3" FontWeight="Bold"
                                   Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnRegister_Click"
                                   Padding="6,4" VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#e3f2fd"/>
                                                            <Setter Property="Foreground" Value="#1565C0"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <!-- Loading Indicator -->
        <Border Name="LoadingPanel" Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="100" Height="10" Margin="0,0,0,10"/>
                <TextBlock Text="로그인 중..." FontSize="14" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>